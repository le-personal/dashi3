<% include ../header.ejs %>

<div id="admin-page">
	<div class="container">
		<h1>
			Applications
			<div class="pull-right">

				<a href="/admin/applications/add" class="btn btn-primary">
					<span class="glyphicon glyphicon-plus"></span> Add application
				</a>
			</div>
		</h1>
		<hr />
		<% include ../../messages.ejs %>
		<% if(results.length === 0) { %>
			<div class="well">No applications added yet. <a href="/admin/applications/add">Add the first one</a></div>
		<% } else { %>
			<table class="table table-stripped table-hover">
				<thead>
					<th>Application name</th>
					<th>Token</th>
					<th></th>
				</thead>
				<tbody>
					<% _.each(results, function(result) { %>
						<tr>
							<td><%= result.name %></td>
							<td><%= result.token %></td>
							<td>
								<a href="/admin/applications/<%= result.id %>" class="btn btn-sm btn-primary">View</a>	
								<a href="/admin/applications/<%= result.id %>/remove" class="btn btn-sm btn-warning">Remove</a>
							</td>
						</tr>
					<% }) %>
				</tbody>
			</table>
		<% } %>
	</div>
</div>